# nginx reverse proxy for Vanilla Pterodactyl Native (panel :80 + daemon :8080)
# Adjust server_name to your FQDN. For NAT VPS, forward 80/tcp and 8080/tcp.
server {
    listen 80 default_server;
    server_name _;

    root __PANEL_DIR__/public;
    index index.php;

    access_log /var/log/nginx/pterodactyl.app-access.log;
    error_log  /var/log/nginx/pterodactyl.app-error.log error;

    client_max_body_size 100m;
    client_body_timeout 120s;

    sendfile off;
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_intercept_errors off;
        fastcgi_buffer_size 16k;
        fastcgi_buffers 4 16k;
    }

    location ~ /\.ht {
        deny all;
    }
}

# WebSocket-aware passthrough for the daemon (optional; panel connects to :8080 directly)
# If you want to serve the daemon behind nginx on a subdomain, add:
# server {
#     listen 8081;
#     server_name daemon.example.com;
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_http_version 1.1;
#     }
# }
